# XKZ-Agent 后端依赖（Python ≥ 3.10）
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0
jieba>=0.42.1
rank-bm25>=0.2.2
numpy>=1.24.0
# v3: Embedding + Reranker（模型不可用时自动降级，这些包非必须）
torch>=2.0.0
transformers>=4.36.0
